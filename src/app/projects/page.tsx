'use client';
import { AppShell } from '@/components/layout/app-shell'; import { ProjectForm, ProjectFormData } from '@/components/forms/project-form'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { projects } from '@/lib/mock-data'; import { currency } from '@/lib/utils';
export default function Projects(){return <AppShell><h1 className="mb-6 text-3xl font-bold">Projects</h1><div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle>Create project</CardTitle></CardHeader><CardContent><ProjectForm onSubmit={(d:ProjectFormData)=>console.log('create project',d)}/></CardContent></Card><div className="space-y-4">{projects.map(p=><Card key={p.id}><CardHeader><CardTitle>{p.name}</CardTitle></CardHeader><CardContent><p>{p.address}</p><p className="text-sm text-muted-foreground">{p.status} · {currency(p.budget)} · {p.diversion_goal}% diversion goal</p></CardContent></Card>)}</div></div></AppShell>}
