import { AppShell } from '@/components/layout/app-shell'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { tasks } from '@/lib/mock-data';
export default function Tasks(){return <AppShell><h1 className="mb-6 text-3xl font-bold">Tasks</h1><div className="grid gap-4 md:grid-cols-3">{['todo','in_progress','blocked'].map(s=><Card key={s}><CardHeader><CardTitle>{s}</CardTitle></CardHeader><CardContent className="space-y-3">{tasks.filter(t=>t.status===s).map(t=><div key={t.id} className="rounded-lg border p-3"><b>{t.title}</b><p className="text-sm text-muted-foreground">{t.priority} · {t.due_date}</p></div>)}</CardContent></Card>)}</div></AppShell>}
