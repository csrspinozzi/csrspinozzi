import Link from 'next/link'; import { ArrowRight, Leaf, Recycle, ShieldCheck } from 'lucide-react'; import { Button } from '@/components/ui/button';
export default function Home(){return <main className="min-h-screen leaf-grid"><section className="mx-auto grid max-w-6xl gap-10 px-6 py-24 md:grid-cols-2"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full bg-secondary px-3 py-1 text-sm"><Leaf size={16}/> Circular construction OS</div><h1 className="text-5xl font-bold tracking-tight">Build homes with less waste, clearer accountability, and measurable circularity.</h1><p className="mt-5 text-lg text-muted-foreground">Manage projects, reclaimed materials, field tasks, documents, storage, realtime updates, and role-based collaboration from one Supabase-backed platform.</p><div className="mt-8 flex gap-3"><Button><Link className="flex items-center gap-2" href="/dashboard">Open dashboard <ArrowRight size={16}/></Link></Button><Button variant="outline"><Link href="/login">Sign in</Link></Button></div></div><div className="grid gap-4">{[['Material passports','Track provenance, embodied carbon, reuse status, and supplier commitments.',Recycle],['Role permissions','Owners, managers, site leads, suppliers, and viewers see only what they need.',ShieldCheck],['Realtime jobsite','Subscribe to project, task, and document updates as teams work. ',Leaf]].map(([t,d,I])=>{const Icon=I as typeof Leaf;return <div key={t as string} className="rounded-2xl border bg-card p-6 shadow-sm"><Icon className="mb-4 text-primary"/><h2 className="font-semibold">{t as string}</h2><p className="text-sm text-muted-foreground">{d as string}</p></div>})}</div></section></main>}
