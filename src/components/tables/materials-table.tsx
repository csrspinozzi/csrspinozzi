'use client';
import { useMemo } from 'react'; import { useReactTable, getCoreRowModel, flexRender, createColumnHelper } from '@tanstack/react-table'; import type { Material } from '@/types/database';
const c=createColumnHelper<Material>();
export function MaterialsTable({data}:{data:Material[]}){const columns=useMemo(()=>[c.accessor('name',{header:'Material'}),c.accessor('category',{header:'Category'}),c.accessor('status',{header:'Status'}),c.accessor('quantity',{header:'Qty'}),c.accessor('circularity_score',{header:'Circularity',cell:i=>`${i.getValue()}%`})],[]); const table=useReactTable({data,columns,getCoreRowModel:getCoreRowModel()}); return <div className="overflow-hidden rounded-xl border bg-card"><table className="w-full text-sm"><thead className="bg-muted/70">{table.getHeaderGroups().map(h=><tr key={h.id}>{h.headers.map(x=><th key={x.id} className="px-4 py-3 text-left">{flexRender(x.column.columnDef.header,x.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.map(r=><tr key={r.id} className="border-t">{r.getVisibleCells().map(cell=><td key={cell.id} className="px-4 py-3">{flexRender(cell.column.columnDef.cell,cell.getContext())}</td>)}</tr>)}</tbody></table></div>}
